<Window x:Class="MentorSpeedDatingApp.Views.MatchingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MentorSpeedDatingApp.Views"
        mc:Ignorable="d"
        xmlns:vm="clr-namespace:MentorSpeedDatingApp.ViewModel"
        xmlns:rsc="clr-namespace:MentorSpeedDatingApp.Resources"
        xmlns:userControls="clr-namespace:MentorSpeedDatingApp.Views.UserControls"
        Title="Mentor-Speed-Dating Matching-Ergebnisse">
    <Window.DataContext>
        <vm:MatchingViewModel />
    </Window.DataContext>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
                <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Style="{StaticResource DataTemplateTimeHeaderTextBlockStyle}"
                               Text="Uhrzeit" />
                    <ItemsControl Grid.Row="1" ItemsSource="{Binding DateTimes}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <StackPanel Style="{StaticResource DataTemplateTimeStackPanelStyle}">
                                        <TextBlock Style="{StaticResource DataTemplateTimeTextboxStyle}"
                                                   Text="{Binding TimeSlot.Time.TimeOfDay.Hours}" />
                                        <TextBlock Style="{StaticResource DataTemplateTimeTextboxStyle}" Text=" : " />
                                        <TextBlock Style="{StaticResource DataTemplateTimeTextboxStyle}"
                                                   Text="{Binding TimeSlot.Time.TimeOfDay.Minutes, Converter={StaticResource TimeOfDayMinutesConverter}}" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
        </Grid>
        <Grid Row="0" Column="1">
            <userControls:MatchingsUserControl />
        </Grid>
    </Grid>
</Window>