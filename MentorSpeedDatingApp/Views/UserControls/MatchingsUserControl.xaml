<UserControl x:Class="MentorSpeedDatingApp.Views.UserControls.MatchingsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MentorSpeedDatingApp.Resources"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:MentorSpeedDatingApp.ViewModel">
    <UserControl.DataContext>
        <vm:MatchingViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Resources/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource HealthCareGreenDarkBackgroundBrush}">
        <ItemsControl ItemsSource="{Binding Matchings}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"
                                Background="{StaticResource HealthCareGreenDarkBackgroundBrush}" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Style="{StaticResource DataTemplateMatchingGridStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <TextBox Grid.Row="0" Style="{StaticResource DataTemplateMentorTextBoxStyle}"
                                 Text="{Binding Mentor.Name}" />

                        <ItemsControl Grid.Row="1" Grid.Column="0" ItemsSource="{Binding Dates}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBox Style="{StaticResource DataTemplateMenteeTextBoxStyle}"
                                                 Text="{Binding Mentee.Name}" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>